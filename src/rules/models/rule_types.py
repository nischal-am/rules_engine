"""
Enumerator for available rule engine types.

Attributes:
    BUSINESS (str): Represents the business rules engine type.
"""
from enum import Enum

class RuleEngineType(Enum):
    """Enumerator for available rule engine types."""
    BUSINESS = "business-rules"

